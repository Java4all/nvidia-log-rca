# Log-RCA

Upload a **`.log`** or **`.txt`** file, then ask questions about failures, errors, and root cause.

**Shortcuts:** use **Replace log** to swap files, **Export last report** after an analysis finishes, or click a suggested question to run the pipeline again.
