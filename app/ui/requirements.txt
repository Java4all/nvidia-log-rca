chainlit==1.3.2
httpx==0.28.1
# asyncer (chainlit) imports sniffio; ensure present when resolver omits it
sniffio>=1.3.0
# Chainlit 1.3.x fails on Pydantic >=2.10.2 (CodeSettings / Action forward refs).
pydantic>=2.7.2,<2.10.2
