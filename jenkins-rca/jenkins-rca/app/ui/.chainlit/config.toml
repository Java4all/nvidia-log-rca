[project]
name = "Jenkins RCA — BAT.AI"

[UI]
name = "Jenkins RCA"
description = "Self-corrective multi-agent RAG for Jenkins log root-cause analysis"
default_collapse_content = true
# Show intermediate steps expanded by default so QA engineers see the pipeline
show_readme_as_default = false

[features]
multi_modal = true
prompt_playground = false
# Enable action buttons (used for quick question shortcuts)
action_buttons = true

[meta]
generated_by = "1.3.0"
